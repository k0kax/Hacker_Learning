# metasploit模块命令

## 1.advanced高级模块管理

在使用一个模块后可以通过，davanced进行模块的高级管理

## 2.loadpath/reload_all外部模块加载

设置加载路径`loadpath` 路径 

重新加载模块 `reload_all`

## 3.previous使用之前的模块作为当前会话模块

## 4.模块栈的加载使用pushm popm

`pushm`将当前模块压入栈，`popm`将栈释放





